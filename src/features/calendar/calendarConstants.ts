/** Ancien slot carrousel — le hub back-office n’est plus une page dédiée. */
export const CALENDAR_INTEGRATION_SLOT_INDEX = 0;

/** Durée par défaut des événements exportés (ms). */
export const CALENDAR_DEFAULT_EVENT_DURATION_MS = 60 * 60 * 1000;
