import { BACKOFFICE_HUB_PAGE_INDEX } from "@/features/backoffice/backofficeConstants";

/** Calendrier vit dans le hub back-office (panneau droit). */
export const CALENDAR_INTEGRATION_SLOT_INDEX = BACKOFFICE_HUB_PAGE_INDEX;

/** Durée par défaut des événements exportés (ms). */
export const CALENDAR_DEFAULT_EVENT_DURATION_MS = 60 * 60 * 1000;
